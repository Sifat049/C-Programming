#include <stdio.h>
#include <stdlib.h>

int main()
{

   double x=10.5;
   double result =log(x);
   printf("log(%lf): %lf",x,result);
 /*double x;
   printf("X=");
   scanf("%lf",&x);

   printf("log(x):%lf",log(x));*/
    return 0;
}
